const std = @import("std");
const tdljson = @import("tdljson.zig");

test "TelegramClient.create and destroy" {
    return error.SkipZigTest;
}

test "TelegramClient.send does not crash" {
    return error.SkipZigTest;
}

test "TelegramClient.receive returns nullable" {
    return error.SkipZigTest;
}